package com.mycompany.booktrack;

public interface StatsCard {
    void loadData();  // Method to load data for the card
    void updateView(); // Method to update the view with the loaded data
}